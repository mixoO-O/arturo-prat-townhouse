export enum HeaderType {
  medium,
  full,
}

export enum ImageAnimation {
  movement,
  zoomOut,
}

export enum TitlePosition {
  topRight,
  topLeft,
  centerRight,
  centerLeft,
  bottomRight,
  bottomLeft,
}
